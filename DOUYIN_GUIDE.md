# 抖音小游戏 / 小程序上传与上线完整操作指南

本项目的全部前端与实时对战后端已经编译打包完毕，并已为您生成好即开即用的**抖音开发者工具专用工程包**。

---

## 目录与文件说明

1. **`douyin-miniprogram/`**：抖音开发者工具工程目录
   - `project.config.json`：工程配置文件（内含 AppID、编译配置）
   - `app.json` / `app.js`：全局窗口与生命周期配置（设置全屏、隐藏多余原生标题栏）
   - `pages/index/`：WebView 容器页面，默认已挂载当前游戏的在线运行环境
2. **`dist/`**：H5 生产环境静态打包文件（`index.html` 与编译后的 `assets/`）
3. **`public/douyin_quoridor_package.zip`**：一键打包好的整包压缩文件（下载即可在电脑解压使用）

---

## 上传与上线实操步骤（4步走）

### 第一步：注册并获取抖音 AppID
1. 打开 [抖音开放平台官网](https://developer.open-douyin.com/)（字节跳动开发者平台）。
2. 点击右上角登录并完成开发者认证（个人或企业均可）。
3. 进入「控制台」-> 点击「创建应用」-> 选择「小程序」或「小游戏」。
4. 创建成功后，在「开发配置」或「应用信息」中复制你的 **AppID**（通常为 `tt` 开头）。

### 第二步：配置业务域名（极为关键）
因为游戏使用 WebView 容器加载在线服务及 WebSocket/SSE 联机服务，需要在平台报备合法域名：
1. 在开放平台左侧菜单进入 **「开发」 -> 「开发配置」 -> 「服务器域名」**。
2. 将以下地址填入 **「request 合法域名」** 与 **「web-view 业务域名」**：
   - 现成测试地址：`ais-pre-hlxfy74xzfnmb5ltzft6an-205701171224.asia-southeast1.run.app`
   - （若后续绑定自己的服务器域名，填写自己的独立域名即可，需支持 HTTPS）

### 第三步：使用「字节跳动开发者工具」导入工程
1. 下载并安装官方 **[字节跳动开发者工具](https://developer.open-douyin.com/docs/resource/zh-CN/mini-app/develop/developer-instrument/developer-instrument-update-and-download)**（支持 Windows / Mac）。
2. 打开开发者工具，选择 **「导入项目」**：
   - **项目目录**：选择解压后的 `douyin-miniprogram` 文件夹。
   - **AppID**：填入你在第一步中获取的 AppID。
3. 导入后，工具中间的模拟器将立即加载出游戏，支持直接点击、滑动测试对战与音效。

### 第四步：上传版本并提交审核
1. 在开发者工具右上角点击 **「上传」** 按钮。
2. 填写版本号（如 `1.0.0`）与更新说明（如「步步为营初版上线」）。
3. 上传成功后，返回 [抖音开放平台网页端后台]，进入「版本管理」。
4. 此时会看到刚上传的开发版本：
   - 点击 **「体验版」**：平台会生成一个体验二维码，你可以用自己的抖音 App 扫码在手机上实机体验！
   - 点击 **「提审」**：填写游戏说明与分类，提交给官方审核，通常 1~2 个工作日即可正式过审上线。
